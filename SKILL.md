---
name: kakao-news-bot
description: 카카오톡 메신저봇R API2와 Windows Python 서버, Gemini로 구성된 뉴스봇을 유지보수하거나 복구할 때 사용한다. 방 필터 불일치, 응답 없음, 뉴스 시간초과, 예약 발송 세션 문제를 로그로 진단한다.
---

# 카카오 뉴스봇 운영

정상 확인된 구성을 보존하며 변경한다. 이 스킬은 앱 재설치, 방 참가, 메시지 발송에 대한 새로운 권한을 부여하지 않는다. 현재 사용자가 요청한 범위에서 작업한다.

## 작업 시작

- [운영 참고](references/operating-notes.md)를 읽고 실제 프로젝트 위치, 설정, 버전을 로컬 파일과 로그로 확인한다. 이 저장소에는 특정 사용자의 배포 정보가 없다.
- 비밀키는 로컬 설정에서 읽고 출력하거나 공유 자료에 복사하지 않는다.
- 기록이나 문서 변경만 요청받았다면 운영 서버와 휴대폰을 재시작하지 않는다.

## 유지해야 할 동작

1. API2 MESSAGE 이벤트의 channelId를 문자열로 비교해 허용된 방을 식별한다. 발신자 이름이나 화면의 제목으로 방 필터를 넓히지 않는다.
2. 허용된 방의 실제 Message 객체를 activeReplyMessage에 보관한다. 명령어가 아닌 일반 메시지에서도 갱신한다. 검증된 대기열 전송 경로는 activeReplyMessage.reply(String(item.message))이다. bot.send(channelId)나 임의로 복원한 숫자 ID가 같은 발송 세션을 제공한다고 가정하지 않는다.
3. activeReplyMessage는 null로 시작한다. 실제 수신 이벤트가 없으면 대기열을 가져오지 않는다. 저장된 방 ID는 재시작 후 살아 있는 답장 세션을 대신하지 않는다.
4. 최상위에서는 이벤트와 타이머를 등록하며 즉시 pollOutbox()를 호출하지 않는다. 해당 초기 호출을 제거한 뒤 컴파일과 응답이 회복된 관측이 있다.
5. 뉴스 요청은 즉시 접수 응답 → 백그라운드 생성 → SQLite outbox 저장 → 휴대폰 폴링 → reply 결과가 true일 때 ack 순서를 유지한다. 생성 전체를 HTTP 요청 안에서 기다리지 않는다.
6. 발송 실패에는 ack하지 않는다. 예약 만료와 재시도를 보존한다. 전송 직후 ack 유실로 중복될 수 있으므로 정확히 한 번 전송을 보장하지 않는다.
7. 뉴스는 Markdown 장식 없는 한국어 일반 텍스트로 보낸다. 공식 원문, 게시일, 핵심 내용, 영향, 링크를 우선한다. 제목만 수집한 후보를 본문 확인이나 교차검증으로 표현하지 않는다.

## 진단 순서

- 서버 /health, /chat 수신, 실제 운영 DB의 outbox를 확인한다.
- 휴대폰 GLOBAL_LOG.json의 컴파일 시작과 완료를 모두 확인한다. 파일 복사나 버튼 클릭만으로 활성화됐다고 보고하지 않는다.
- 봇 log.json에서 관련 MESSAGE, 방 ID, HTTP 응답, 예외만 확인한다. 관련 없는 대화를 출력하지 않는다.
- 이벤트가 없으면 알림 접근 권한, 카카오톡 앱과 방 알림, 봇 활성화 상태를 점검한다. 열린 채팅방 때문에 알림이 발생하지 않는지도 확인한다.
- 이벤트는 있지만 /chat이 없으면 방 필터와 호출어를 확인한다. /chat은 있지만 답장이 없으면 요청 시간과 reply 결과를 확인한다.
- ping, 뉴스 생성, outbox 전송을 구분해 검증한다. 반복 재시작은 보관된 Message 세션을 지울 수 있다.

## 완료 기준

코드 변경 시 배포 파일 일치와 컴파일 완료를 확인한다. 허용된 방의 실제 수신, 접수 답장, 최종 뉴스 발송을 필요한 범위에서 검증한다. ack는 클라이언트의 성공 보고이며 실제 화면 확인과 구분한다. 새 알림이 필요하면 사용자에게 필요한 메시지를 한 번 안내한다. 미래 예약 실행이나 재부팅 복구를 시험하지 않았다면 검증 완료로 표현하지 않는다.
